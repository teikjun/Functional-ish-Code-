import cs2030.mystream.InfiniteList;
import cs2030.mystream.InfiniteListImpl;

//import java.util.stream.IntStream;
//import java.util.Arrays;

class Main { 
    public static void main(String[] args) {
        //InfiniteList<Integer> myStream = InfiniteList.generate(() -> 1);
        //InfiniteList<Integer> myStream = InfiniteList.iterate(1, x -> x + 1).limit(5);
        //System.out.println(Arrays.toString(myStream.toArray()));
        //System.out.println(myStream.reduce(0, (x,y) -> x + y));
        //myStream.forEach(x -> System.out.println(x));
        //System.out.println(myStream.count());
        //IntStream.range(0,4).forEach(x -> System.out.println(myStream.get()));
    }
} 
